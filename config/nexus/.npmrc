# Конфигурация NPM для работы с Nexus репозиторием
# Согласно инструкции QazTech, шаг 15.6
#
# Использование:
#   Скопировать этот файл в корень проекта или в ~/.npmrc
#   Заменить <PROJECT_NAME> на имя вашего проекта
#   Установить переменные окружения NEXUS_USERNAME и NEXUS_PASSWORD

# URL Nexus репозитория на платформе QazTech
registry=https://nexus.qaztech.gov.kz/repository/<PROJECT_NAME>-repo/

# Включить аутентификацию
always-auth=true

# Токен аутентификации (base64 encoded username:password)
# Генерируется командой: echo -n "username:password" | base64
# _auth=${NEXUS_AUTH_TOKEN}

# Альтернативный способ - через переменные окружения:
# NPM_CONFIG_REGISTRY=https://nexus.qaztech.gov.kz/repository/<PROJECT_NAME>-repo/
# NPM_CONFIG_ALWAYS_AUTH=true
# NPM_CONFIG__AUTH=${NEXUS_AUTH_TOKEN}

